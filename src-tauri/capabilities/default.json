{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "default capability",
  "windows": ["main", "floating", "import", "selector"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default",
    "global-shortcut:default"
  ]
}
